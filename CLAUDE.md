# Discord常駐Claudeの申し送り

- 会話履歴は `session.json` で管理し、旧ログを削除しない。
- `!new` は要約付き、`!new fresh` は要約なしで新規セッションへ切り替える。
- 20依頼または推定80,000トークンで、次の依頼前に自動更新する。
- 旧会話の情報は秘密を除いた3,000文字以内の要約だけを新会話へ渡す。
- 要約失敗時は旧セッションを維持する。
- 本番反映は `com.cocoa-m3.claude-discord-bot` LaunchAgentの再起動後に確認する。
